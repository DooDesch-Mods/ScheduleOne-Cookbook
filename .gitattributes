# Normalize line endings: keep LF in the repo, let Git handle working-copy conversion.
* text=auto eol=lf
# Treat these as binary (no eol conversion).
*.png binary
*.jpg binary
*.gif binary
*.webp binary
